# Environment variables for the local Docker stack.
# Copy to an .env used by docker compose to override defaults.
# Not for production secrets — production uses a secret manager.

# Postgres
POSTGRES_USER=ajac
POSTGRES_PASSWORD=ajac_dev_password
POSTGRES_DB=ajac
POSTGRES_PORT=5432

# Adminer
ADMINER_PORT=8081